[package]
name = "contra-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tetanes-core = "0.14"
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"
getrandom = { version = "0.4", features = ["wasm_js"] }
