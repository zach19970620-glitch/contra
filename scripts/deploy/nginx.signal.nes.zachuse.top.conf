# 仅信令 — 前端在 Cloudflare Pages，不在此服务器托管静态文件
# sudo certbot certonly --nginx -d signal.nes.zachuse.top
# sudo cp scripts/deploy/nginx.signal.nes.zachuse.top.conf /etc/nginx/sites-available/contra-signal

server {
    listen 80;
    listen [::]:80;
    server_name signal.nes.zachuse.top;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name signal.nes.zachuse.top;

    ssl_certificate /etc/letsencrypt/live/signal.nes.zachuse.top/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/signal.nes.zachuse.top/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location /ws {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }

    location / {
        return 404;
    }
}
